import { Head, Link, router } from '@inertiajs/react';
import AdminLayout from '@/Layouts/AdminLayout';
import { Download, FileText, Pencil, Plus, Trash2 } from 'lucide-react';
export default function Index({ dokumens }) { const remove=(id)=>confirm('Hapus dokumen ini?')&&router.delete(`/admin/dokumen/${id}`); return <AdminLayout title="Dokumen Publik"><Head title="Dokumen Publik"/><div className="mb-7 rounded-xl bg-gradient-to-r from-primary-dark to-primary p-6 text-white"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-center"><div><p className="text-xs font-bold uppercase tracking-widest text-accent">Transparansi Desa</p><h2 className="mt-1 text-2xl font-extrabold" style={{fontFamily:'var(--font-heading)'}}>Dokumen Publik</h2><p className="mt-1 text-sm text-white/75">Kelola dokumen yang dapat diunduh warga.</p></div><Link href="/admin/dokumen/create" className="btn bg-white text-primary hover:bg-primary-bg"><Plus size={17}/> Unggah Dokumen</Link></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{dokumens.data.map((d)=><article key={d.id} className="group rounded-xl border border-border-light bg-white p-5 shadow-subtle transition hover:-translate-y-0.5 hover:shadow-card"><div className="flex items-start justify-between"><div className="rounded-lg bg-primary-bg p-3 text-primary"><FileText size={24}/></div><span className={`rounded-full px-2.5 py-1 text-xs font-bold ${d.is_publik?'bg-green-100 text-green-800':'bg-gray-100 text-gray-600'}`}>{d.is_publik?'Publik':'Draft'}</span></div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-primary">{d.kategori} · {d.tahun}</p><h3 className="mt-1 min-h-12 font-extrabold leading-snug text-charcoal" style={{fontFamily:'var(--font-heading)'}}>{d.judul}</h3><div className="mt-5 flex gap-2 border-t border-border-light pt-4"><a href={`/storage/${d.file}`} target="_blank" className="btn btn-secondary flex-1 px-3 py-2"><Download size={15}/> Lihat</a><Link href={`/admin/dokumen/${d.id}/edit`} className="rounded-md p-2 text-primary hover:bg-primary-bg"><Pencil size={17}/></Link><button onClick={()=>remove(d.id)} className="rounded-md p-2 text-red-600 hover:bg-red-50"><Trash2 size={17}/></button></div></article>)}{!dokumens.data.length&&<div className="col-span-full rounded-xl bg-white py-16 text-center text-text-muted">Belum ada dokumen. Mulai dengan mengunggah dokumen pertama.</div>}</div></AdminLayout>; }
